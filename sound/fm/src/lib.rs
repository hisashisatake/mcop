//! fm-common: FM合成チップ間（`ym38x6-core`/`op505-core`）で共有する、EG非依存の汎用部品。
//!
//! アルゴリズム結線表・TL/KSR等のパラメーターマッピング・波形生成は
//! どのEG方式（レート方式/N点Time-Level方式）を採るFM合成チップにも共通して使える。
//! EGそのもの（`sound_core::Eg`/`TimeEg`）や各チップ固有のオペレーター/チャンネル実装は
//! ここに置かず、各チップのコアクレートに残す。
//!
//! 旧`chip_lfo`モジュール（実機LFOレジスタ値の変換テーブル）は、ym38x6削除後は
//! 実際の直接利用者がop505グループに閉じたため、2026-09-01に`op505-core::modulation_curves`
//! へ移設した（本クレートは製品非依存の共有基盤という位置づけのため、op505専用になった
//! コードを置き続ける理由が無くなったことによる）。

pub mod algorithm;
pub mod mapping;
pub mod waveform;
