[package]
name = "gesture-app"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
op505-core = { path = "../../op505/core" }
serde = { version = "1", features = ["derive"] }
