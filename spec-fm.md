# FM音源変換ツール群 横断知見

[spec-sound.md](spec-sound.md)が38x6エンジン自体の仕様であるのに対し、本ドキュメントは
**レガシーFM音源チップの音色を38x6へ取り込むコンバーター群**（`ym38x6/tools/opz2x6`・`ym38x6/tools/opm2x6`・
`ym38x6/tools/psr2x6`・`ym38x6/tools/mucom2x6`・`ym38x6/tools/vgm2x6`、および検証用の`ym38x6/tools/opzref`）が積み上げてきた、
各チップ固有の解析知見・変換設計判断・その根拠を集約する。個別ツールのCLI使い方はCLAUDE.mdおよび
各`private/`配下のメモを参照。ここでは**なぜその変換式・その値になったか**を記録する。

設計判断の詳しい経緯は`docs/session_history.txt`にも一部残っている。ここは横断的な参照用の要約。

---

## 1. 設計哲学（全コンバーター共通）

### 38x6の立ち位置＝「OPN/OPMのFMコア」＋「拡張軸」

- **共通（検証済みの骨格）**: アルゴリズム8種（ymfm OPN `s_algorithm_ops`移植）・オペレーター配線
  （slotインターリーブ）・EG/FMコアはOPM同等。音程・配線・基本EGはOPM知識がそのまま通用する
  （OPZ/OPQの音程バグもOPM理屈で解けた実績がある）。
- **拡張軸（38x6の個性、前向きに投資する所）**: 全パラメーター8bit化（実機は4〜7bit）／非サイン波形
  （OPZ由来サイン8種＋独自拡張のノコギリ/矩形/三角＝ビルトイン32波形）／SVFフィルター／
  MasterEffects／リッチLFO・EG／op_fine_tune／無制限ポリ。
- 結論: 「OPMの上位互換だが、拡張が音の支配項なので聴感は別キャラクター」。**エミュレーターではなく
  独自シンセ**。

### 層分離の原則（忠実性と味付けを二者択一にしない）

「あるチップの再現度を上げると別チップの再現が崩れる」というジレンマは、調整する層を分ければ起きない。

- **エンジン（sound-core / ym38x6-core）＝ OPN/OPM忠実なコア**。各チップの都合で触らない。
  `FM_MODULATION_INDEX_SCALE`等のグローバル定数を特定チップの味付けのために弄るのは誤り
  （psr2x6が一時中断した際の失敗パターンがこれだった。詳細は4章）。
- **コンバーター＝チップ別の味付けが住む場所**。`--mod-cap`/`--sustain`/`--cutoff`等は1変換に閉じ、
  エンジンも他チップも汚さない。OPZ/OPQ側をいくら詰めても`opm2x6`（OPN/OPM再現）は不変。
- どうしてもエンジンに手を入れる場合のみ、OPN/OPM非回帰を`opm2x6`/`mucom2x6`リファレンスで
  担保してから行う。
- 評価軸は「元チップの音と一致するか」ではなく「**38x6のエンジンで良い音になっているか**」。
  コンバーターは「エミュ」ではなく、レガシー音色を38x6という“より広い楽器”へ取り込む
  **インポーター**として設計する。

---

## 2. エンジン共通のパラメーターカーブ設計手法

### ymfm理論値アンカー＋指数補間

0〜255の連続パラメーター（AR/D1R/D2R/RR、feedback、KSR等）は、**両端をymfmソース解析で求めた
実機OPM/OPNの理論極値に固定し、間を指数補間する**設計手法を採用している。以前は2乗カーブ等で
低〜中域の変化が「死んでいる」状態だったが、この手法に変えたところ聴感で明確に「FM音源っぽさが
増した」という評価を得た（ユーザー確認済み）。

| パラメーター | 範囲 |
|---|---|
| AR | 0.68ms〜20.2秒（`rate=0`はフリーズ例外） |
| D1R/D2R/RR | 8.71ms〜284.9秒（`rate=0`はフリーズ、RRのみ実機にない「リリースしない」拡張） |
| feedback | 0=完全オフ、1〜255で7オクターブ、最大1.8サイクルまで指数的に増加（帰還方式・経緯は後述「フィードバック帰還」参照。旧max=8.0はループゲイン過大でノイズ過多と判明し却下済み） |
| KSR | 1オクターブあたり約1.09倍(0)〜2倍(255)、1段ごとに倍になる指数カーブ |

**新しいパラメーターを追加する際の手順**:
1. `vendor/ymfm`のソースで実機のレジスタ→内部値の変換式を確認する
2. 「1段ごとに倍」「対数的」等の指数的性質がないか調べる
3. 両端を実機理論値にアンカーした指数カーブを提案し、具体的な数値テーブルで現状との差を示す
4. ユーザーに確認してから実装・テスト・[spec-sound.md](spec-sound.md)への反映を行う

### フィードバック帰還: 2サンプル平均（既定）+ 上限1.8

`feedback_to_scale`の最大値は当初OPN理論値0.5サイクルに絞っていたが、MUCOM88変換音色
（@113 HI_HAT）のノイズ成分が再現できない問題があり、**1.8**（FB=7相当で約1.7サイクル）へ
引き上げた（`mucom2x6`、2026-06-17）。帰還方式自体は当初**直近1サンプル**（`out[n-1]`のみ）
だったが、2026-07-18に**2サンプル平均**`(out[n-1]+out[n-2])/2`（実機OPM/OPN/OPZ準拠）へ
既定を変更した（`ym38x6-core`の`FEEDBACK_TWO_SAMPLE`、`set_feedback_two_sample(false)`で
旧1サンプルに戻せる）。

- **2026-06-20時点の誤判定**: 当時「2サンプル平均は1サンプルとほぼ完全一致し効果なし」と
  判定したが、これはopz2x6の`mod_tl_cap=180`が変調を潰した状態での測定だった。
- **2026-07-18の再評価**: opz2x6のモジュレーターTL天井を撤廃（後述）した上でopzref
  (ymfm実機参照)基準に再測定したところ、2サンプル平均の方がハイハット系のノイズ忠実度
  （D023 FM Hi-Hats: スペクトル平坦度が1サンプルの0.236→2サンプルの0.31、実機0.339に接近）・
  OPNベースのピッチ安定性（Hydlide3変換ベース: 自己相関の明瞭ピッチ窓が1.7%→38.9%に改善）
  ともに1サンプルより実機に近いと判明し、既定を2サンプル平均へ変更した。
- **OPNベースの音程溶けは完全解決ではない**（38.9%、実機相当の0.5サイクル帰還時の72.4%には
  届かない）。現状はエンジン既定の改善で留め、ボイス単位のFB圧縮（コンバーター層）が必要かは
  今後のベース系変換の様子を見て判断する（層分離の原則どおり、対応するならエンジンでなく
  コンバーター側）。

### KSRカーブ: 線形化＋クランプ撤去（2026-07-02、`ksr_rate_multiplier`）

以前は`ksr_rate_multiplier`が指数カーブ＋A4未満`.max(1.0)`クランプ（低音側を実機より遅くし
すぎる効きすぎをクランプで打ち消す設計）で、「クランプ撤去はMUCOM88実機の聴感から離れる」
としてクランプ維持が結論だった。

2026-07-02、opzref(ymfm実機参照)とのGrandPiano EGカーブ比較を機に、**クランプ撤去単体では
なく**KS(0〜3)→ksrの写像を全コンバーター（opz2x6/opm2x6/mucom2x6/psr2x6の`ks_to_ksr`関数、
共通テーブル`[0, 64, 128, 255]`）で統一する形で再設計し、この2つを両立させた:

- エンジン側`ksr_rate_multiplier`は`exponent = ksr/255`の**単純な線形カーブ・クランプなし**
  （A4未満で1.0を下回ることを許容）に変更。実機KSの概念に依存しない汎用カーブに徹する。
- KS→ksrの対応（KS=0→ksr=0=完全フラット、KS=2→128≈exponent 0.5・GrandPianoキャリア実機
  比較で検証済み、KS=3→255=理論値）という**味付けは各コンバーター側の責務**とする
  （層分離の原則）。

GrandPiano実機比較（ユーザー聴感）で「モジュレーター由来の倍音が別音として浮く」問題が解消。
残差（減衰速度が実機より約1.7〜1.9倍遅い）は`KEY_CODE_A4=19`の較正など別要因の可能性があり
未調査（次回`git blame`で導入経緯を追う）。

### キャリアTLとモジュレーターTLの独立性

`operator.rs`の`tick()`実装より: モジュレーターの`modulation`（位相オフセット）は自分の位相へ
加算された**後**に、自分のTLは波形サンプルを引いた**後**に音量として掛かる。つまり
**モジュレーターTL（音色・明るさ）とキャリアTL（音量）は完全に独立**しており、キャリアTLだけを
変えても音色は壊れない。試聴系ツールがノート単位でピーク正規化していたため、この独立性が
聴感で見えず「ブラス族全体が静か」という問題を長らく見落としていた（2026-06-30、正規化除去で発覚・
commit 442a897）。**新しい音色族テンプレートを作る際はピーク正規化しないこと**（音量差が試聴で
分かるようにする）。

### 複数キャリア合算のヘッドルーム補正

アルゴリズムが複数キャリアを並列合成する場合、各キャリアのTLをそのまま単一キャリア相当で
出力すると、合算後にオーバーシュート/実機よりも大きすぎる音量になる。この現象は独立した
2つの調査で一致した値が得られている:

- **手動設計パッチ（`patchlab`）での経験則**（2026-06-30）: 2キャリア合算のアルゴリズムでは
  各キャリアのTLを単一キャリア時より**-14〜-18**（38x6 TLスケール、0.373dB/step）程度
  引く必要があった（ピアノAlg4等）。
- **TX81Zの実機仕様Aalg**（`opz2x6`、2026-07-02、詳細は4.3節）: 2キャリアのアルゴリズムは
  TLレジスタに**+8**（TX81Z 0.75dB/stepスケール）の追加減衰が乗る。38x6 TLスケールに換算すると
  **+16**。

38x6スケールで-14〜-18（経験則）と16（実機由来の理論値）はほぼ一致しており、
「並列キャリア合成には`-20*log10(合算数)`dB程度のヘッドルーム補正が必要」という現象が
チップ非依存の物理法則（位相加算時の最悪ケース振幅）であることを裏付けている。

---

## 3. TX81Z / YM2414 (OPZ) — `opz2x6` / `opzref`

TX81Zはヤマハの4オペレーターFM音源モジュール（1987年）。YM2151(OPM)とほぼ同じFMコア＋
非サイン波形8種・固定周波数モード等の拡張を持つ。

### 3.1 ボイスデータフォーマット

- **VCED**（Voice Edit Parameters）: DX100等OPP系機種と共通の標準パラメーター形式
- **ACED**（OPZ拡張パラメーター）: 波形選択・固定周波数モード等、OPZ固有の拡張
- **VMEM**（実ファイル検証済み、`opz2x6/src/parse.rs`）: 10バイト/OP、フィールド順
  AR/D1R/D2R/RR/D1L/LS/AME_EBS_KVS/OUT/FREQ/RS_DBT。OP格納順は**OP4(0-9), OP2(10-19),
  OP3(20-29), OP1(30-39)**——連番でなくインターリーブされている点に注意。グローバルは
  addr40（SY|FBL|ALG packed）、addr41〜44（LFS/LFD/PMD/AMD）、addr45（PMS|AMS|LFW packed）、
  addr57-66（音色名）。EGTはVMEMに存在せずデフォルト0（sustain）。

### 3.2 オペレーター結線順序（決着済み）

TX81Z（OPP系チップ）はOPN/OPM系と結線順が逆（OPN/OPM=1→2→3→4、OPP=4→3→2→1）。加えて
OPZ(YM2414)はOPM系レジスタなので、slot1↔slot2のインターリーブが物理的に入る
（ymfm `opz_registers::operator_map`: m_op=[slot0,slot2,slot1,slot3]）。

VMEMファイルはバイトオフセット順で OP4(0-9)/OP2(10-19)/OP3(20-29)/OP1(30-39) と書かれており、
これはバルクダンプがチップ内部レジスタをそのまま反映したもの——つまり物理slot0=OP4,
slot1=OP2, slot2=OP3, slot3=OP1に対応する。よって`m_op = [slot0, slot2, slot1, slot3]
= [OP4, OP3, OP2, OP1]`はVMEM格納順`ops[]`**そのもの**（恒等写像）になる。

```rust
const OP_SRC: [usize; 4] = [0, 1, 2, 3]; // operators[i] ← ops[OP_SRC[i]]（恒等写像）
```

**検証**: 24通りのslot順列を実機録音のスペクトルと比較する形で総当たり検証し、恒等写像が
実機（GrandPiano、LoTine81Z）と一致することを確認済み（2026-06-21）。2026-07-01に一度
「恒等写像`[0,1,2,3]`ではなく逆順`[3,1,2,0]`が正しいのでは」という矛盾が生じたが、これは
参照レンダラ`opzref`自体のKCオクターブ境界バグ（+1半音、3.6節）が原因で、修正後は
恒等写像が実機録音と一致することを再確認した（2026-07-01、コミットa92b0dd）。

### 3.3 出力レベル（TL）変換

TX81Zファームウェアは、実機のTLレジスタ（`0x60`〜`0x7F`、7bit、0=最大出力・127=最小）へ
書き込む値を次の式で計算する（出典: nornandブログ「TX81Zを解析した（Operator Output Level編）」
https://nornand.hatenablog.com/entry/2020/11/21/201911 、TX81ZシステムROM解析による実測値）:

```
V_TL = 0 + A_vol + A_alg + A_ol + A_ls + A_kvs + A_ebs
```

`opz2x6`が実装しているのは`A_ol`と`A_alg`（単一ボイス変換に直接関わる項）。`A_vol`（Performance
モード限定）・`A_ls`／`A_kvs`／`A_ebs`（音色データではなく演奏時の動的パラメーター）は現状
未実装（3.4節参照）。

#### A_ol — Operator Output Level

VCED/VMEMの`OUT`フィールド（0-99、99=最大）から、実機TLレジスタへの加算値`Aol`（0-127、
0=最大）への変換。**OL=20以上は1刻みの線形**（`Aol = 99 - OL`）だが、**OL=19以下は急激に
非線形化**する（無音側を急に絞る）。

```
OL=99→Aol=0, OL=79→Aol=20, OL=20→Aol=79   ← ここまで線形
OL=19→Aol=81, OL=10→Aol=96, OL=0→Aol=127  ← ここから非線形（2刻み以上に拡大）
```

完全な100エントリテーブルは`opz2x6::conv::OL_TO_AOL`（`ol_to_atten()`で参照）。旧実装
（`out/99*254`の単純線形近似）はこの非線形域を再現できていなかった（2026-07-02に修正）。

`Aol`（0-127、OPM系TLは0.75dB/step≈95.25dB幅）から38x6のTL（0-255、0.373dB/step≈同じ
95.25dB幅、ちょうど2倍解像度）への変換は、両者の幅が揃っているため単純な向き反転＋
リスケールで済む: `tl_38x6 = round((127 - Aol) / 127 * 255)`。

#### A_alg — アルゴリズムによる追加減衰（キャリアのみ）

同記事に、アルゴリズムごとの追加減衰量が明記されている:

| アルゴリズム（表示1-8） | 対象オペレーター | 減衰量 |
|---|---|---|
| 1,2,3,4（単一キャリア） | op1,2,3,4 | 0 |
| 5（2キャリア） | op1,3 | 8（op2,4は0） |
| 6,7（3キャリア） | op1,2,3 | 13（op4は0） |
| 8（4キャリア＝全並列） | op1,2,3,4 | 16 |

この「減衰を受けるオペレーターの集合」は`opz2x6::conv::CARRIERS`（どのoperators[]がキャリアか）
と完全に一致し、減衰量はキャリア本数nに対して`-20*log10(n)`dB（並列合成時の位相加算
ヘッドルーム補正、0.75dB/step換算でほぼ厳密に8/13/16と一致）。モジュレーターと単一キャリアの
アルゴリズムは常に0（2章の「複数キャリア合算のヘッドルーム補正」参照）。2026-07-02に発見・
実装（見落としていた項目をユーザー指摘で発見）。`opz2x6`と`opzref`の両方で
`opz2x6::conv::alg_atten(alg)` / `is_carrier(alg, op_index)`を共有する。

#### A_kvs / A_ls / A_ebs — 導出アルゴリズム（未実装、将来の参照用）

nornandブログ「導出方法を考えてみた」（https://nornand.hatenablog.com/entry/2021/01/01/153911）
が、実測の巨大テーブルをC言語風の疑似コードへ圧縮した導出式を公開している（著者が実測値と
照合済み）:

- `attLS(ls, note)` — Level Scaling（VCEDのLS 0-99とMIDIノート番号から減衰量を返す）
- `attKVS(kvs, velocity)` — Key Velocity Sensitivity（VCEDのKVS 0-7とMIDI velocityから
  減衰量を返す。`kvs==0 ? 0 : ((kvs*table[velocity-1] + (7-kvs)*16)>>3) + 1`、返り値は
  整数部7bit+小数部1bit）
- `attEBS(bias, ebs, cc2)` — ブレスコントロール（VCEDのBCEG Bias・EBSとMIDI CC#2から
  減衰量を返す）

**`attKVS`は`opz2x6`の`velocity_sensitivity`変換の根拠として採用済み**（3.4節）。`attLS`
（ノート依存の音量変化）と`attEBS`（ブレスコン）は、38x6に対応する仕組みが現状なく未使用
（将来Level Scaling相当の機能を実装する際の一次資料として保持）。

### 3.4 velocity_sensitivity（KVS）マッピング

38x6の`velocity_sensitivity`は実機のKVSとは設計思想が異なる。実機はTLレジスタへの直接的な
減衰（キャリア・モジュレーター問わず効く）だが、38x6は**モジュレーターの明るさ専用**
（`effective_tl = base_tl + (velocity/127) * sensitivity`という単純な線形加算、
`ym38x6/core/src/mapping.rs`）。キャリアの音量はベロシティに関わらず常に
`velocity_to_volume_gain`で一本化する設計（「velocity=音量」という38x6独自のポリシー）。

`opz2x6`はこれに合わせ、キャリアは`velocity_sensitivity=0`固定、モジュレーターのみKVSを
写像する:

```
velocity_sensitivity = KVS(0-7) × 24    （キャリアは常に0）
```

**係数24の根拠**（2026-07-02改訂、旧実装は`×10`）: `attKVS`から「弱打(velocity=1)→
強打(velocity=127)」の減衰スイングをKVS=1〜7それぞれで計算すると、厳密に`KVS×12`
（TLレジスタ0-127スケール、0.75dB/step）になる。38x6のTLは同じ約95.25dB幅を255段階
（0.373dB/step、ちょうど2倍解像度）で表すため、換算係数は正確に2倍で`KVS×24`。

旧`×10`は、`255/7`（≈36/step）を試した際に高velocity域でTLがクランプされすぎたために
経験的に抑制した値だった。`×24`は理論値だが、`base_tl`が高いモジュレーターでは依然
クランプの可能性が残っており、**要聴感検証**（2026-07-02時点で未確認）。

### 3.5 周波数比テーブル

TX81ZのFREQ coarse値（0-63）は「4個おきの連続ブロック」のような単純規則ではなく、
**4グループ（グループごとに異なる線形係数）へ複雑にスクランブルされて割り当てられている**。

```rust
const COARSE_TO_GROUP: [(u8, u16); 64] = [...]; // coarse → (group, order_base)
const FREQ_RATIO_COEFFS: [(f32, f32); 4] = [
    (0.50, 0.0625),
    (0.71, 0.088105),
    (0.78, 0.098145),
    (0.87, 0.108105),
];
// ratio = base + step * order_base
```

出典: https://mgregory22.me/tx81z/freqratios.html （実測によるリバースエンジニアリング。
著者自身「端数に説明のつかない周期誤差が残る」と明記しており完全な精度は無いが、旧実装
（単純な等間隔ブロック仮定）より大幅に正確）。旧実装はcoarse値が大きくなるほど誤差が拡大
していた（例: coarse=22で旧実装7.937 vs 実測7.00、+13.4%誤差＝金属的・非整数倍音的な
音色劣化の主因だった）。

### 3.6 EGレートカーブ

AR/D1R/D2R/RR（0-31のOPM型レート）は、2章の「ymfm理論値アンカー＋指数補間」で導出した
カーブをそのまま使用。「LiteHarpsi等で減衰が実機より速すぎるのでは」という疑惑が出たが
（2026-07-01）、以下の検証で撤回・シロと結論した:

1. 実機録音をオンセット検出すると、疑惑の原因は超高速ロール/トレモロ奏法（7.5秒間に284回）
   であり、1音の減衰自体は矛盾しないと判明。
2. 独立資料（deicsonze、https://github.com/ngeiswei/deicsonze 、gnuplotフィッティングに
   よるYM2414系EG式）で裏取り: `envAR2s(31)=10.4423*exp(-0.353767*31)≈0.18ms`
   （ym38x6-coreの0.68msよりさらに速い）等、3点とも独立実装と自分たちの値が同じ方向で
   収束し「直す根拠なし」で決着。

### 3.7 KCオクターブ境界バグ（opzref、修正済み）

`opzref`（TX81ZをymfmのOPZエミュで直接レンダリングする検証ツール）に、系統的に+1半音
シャープするバグがあった。原因は`NOTECODE`テーブルのオフバイワン
（`[14,0,1,2,4,5,6,8,9,10,12,13]` → 正しくは`[0,1,2,4,5,6,8,9,10,12,13,14]`）で、
ymfm/実チップのオクターブ「ブロック」境界がC/C#間ではなく**C#(N)〜C(N+1)の12音**で
区切られていることに起因する。修正はコミット149e4b4→さらに`midi_to_kc()`冒頭で
`midi = midi.saturating_sub(1)`する1行修正（コミットa92b0dd）。実機録音（A01 GrandPiano.mp3）
との聴き比べで再現成功を確認済み（2026-07-01）。

この修正により、3.2節の「オペレーター結線順序」を巡る2026-06-21と2026-07-01の矛盾する
結論が解決した（`opzref`のピッチバグが原因で、修正前のレンダリングでは正しい結線順序でも
ノイズ的に聞こえていた）。

### 3.8 ymfm OPZの信頼性

**opzrefは音程/アルゴリズム配線/オペレーター順序の検算専用とし、音色（TL/EGの細かい聴感）の
正解判定には使わない**。YM2414は非公開チップでymfm OPZの実装は推測主体
（`// QUESTION`コメントが残存）。第三者証言（KQ Tixie開発者インタビュー、
https://www.dtmstation.com/archives/77989.html）でも「vanilla ymfmは再現度がもう一歩」
「低速EGの滑らかさ・OPPのレベルスムージング・OPZのRapid Attackは自前で修正した」と
言及されている。音色の正解は実機録音を優先し、無ければ耳で調整する。

### 3.9 opz2x6の味付けオプション（実機忠実からの意図的な逸脱）

音程・配線が確定した後、「実機忠実」から意図的に離して「楽器として良い音」にするための
オプション群（全て既定OFF、opt-in）:

| オプション | 内容 |
|---|---|
| `--sustain <0.0-1.0>` | キャリアのみD1Lを満レベル方向へ持ち上げ＋D1R/D2Rを減速。エレピ系で伸びを作る |
| `--cutoff <0-255>` | チャンネルLPF。倍音過多の抑制はモジュレーター削減でなく**LPFが正解**（変調を削ると FMサイドバンドが作る基音まで失われ音程感が壊れる） |
| `--mod-cap <N>` | モジュレーターTL天井（**既定は天井なし**。2026-07-02に一度「天井180」を既定にしたが、この判定はD1L極性反転バグ（4章参照）混入下の聴感比較だった疑いが濃厚。2026-07-18にopzref(ymfm実機参照)基準で再評価し、天井なしの方が実機の明るさ・ノイズ感（A028 RichHarpsi等）に近いと判明、既定を天井なしへ差し戻した。`--mod-cap 180`で明示的に旧既定へ戻せる） |
| `--fb <N>` / `--ksr <N>` | フィードバック/KSRの一律上書き（切り分け診断用） |

### 3.10 参照資料一覧

| 資料 | 信頼度 | 用途 |
|---|---|---|
| nornandブログ「TX81Zを解析した」シリーズ（たま日記） | 高（TX81ZシステムROM解析による実測） | A_ol/A_alg/A_vol/A_ls/A_kvs/A_ebs/Instrument Detuneの一次資料 |
| DXConvert（https://dxconvert.martintarenskeen.nl/） | 高（実績のある社外変換ツール） | 周波数比テーブル・EGレート変換表・carrier()テーブルの裏取り |
| deicsonze（https://github.com/ngeiswei/deicsonze） | 中（著者曰く実機比較なし、資料ベース） | EGレート式（AR/D1R/D2R/RR→実時間）の独立裏取り |
| mgregory22.me/tx81z/freqratios.html | 中〜高（実測だが端数誤差の記載あり） | 周波数比テーブル |
| YM2414-LLE（https://github.com/nukeykt/YM2414-LLE） | 潜在的に最高（ダイ解析ベース）だが現状WIP、EG未実装 | 未使用、開発進捗を定期確認する価値あり |
| ym2414b（https://github.com/iflyhigh/ym2414b） | 中 | TLスケーリング・チューニングテーブル（EGタイミングには未使用） |
| SOLID STATE JAPANブログ（個人の音楽制作記録） | 低（著者自身「確証ソースなし、耳で調整」と明言） | 結線順逆転・D1L反転の定性的な傍証のみ |
| KQ Tixie開発者インタビュー（DTMステーション） | 一次証言 | ymfm OPZの音色再現精度の限界について |

---

## 4. PSR-70 / YM3806 (OPQ) — `psr2x6`

PSR-70はヤマハの4オペレーターFM音源搭載パネル楽器。OPQ(YM3806)を搭載。

### 4.1 ROM音色テーブル構造

`ROM2.bin`の68音色×64バイトを`parse_rom2_voices`でデコード。

- voice0-31: 32パネル音色（4op通常音色、実際にパネルから選べる音色）
- voice32-51: 2op複製テーブル（op0==op1 AND op2==op3が平均12/16行、変換品質が低く除外推奨）
- voice52-67: 追加16音色（パネル外の4op音色）

### 4.2 変換式

AR/D1R/D2R/RR/RRはmucom2x6と同じ「KS考慮eg_rate理論カーブ、A4アンカー」（`opq_rate_to_x6`
系）へ移植（単純線形×8を廃止）。AR変換には`ATTACK_ONSET_BIAS=30`（dBリニアアタックの
オンセット遅れ補正）を加算する。OPQのeg_rate/KSR機構はOPM/OPN系と同一前提で扱う
（ユーザー承認、独自裏取りはしていない）。

### 4.3 中断の真因＝mod-capの欠如（解決済み）

PSR-70 ROM音色の試聴でノイズ感が強く、当初「FM変調のクセを減らすため」と誤って
`FM_MODULATION_INDEX_SCALE`（エンジングローバル）を弄って一時中断していた。これは
「層分離の原則」に反する**誤ったレバー**（全コンバーター/エンジンに波及する）だった。

**真因**: `psr2x6`には`opz2x6`が既定で持つモジュレーターTL天井が無かった。PSR-70 ROM音色は
feedback=252が32中17・モジュレーターTL>200が多く、天井なしだとモジュレーターが254まで
振れ、変調指数が過大でノイズ化していた。対処は`opz2x6`と同一思想の味付けオプション追加
（`--mod-cap`既定180、`--sustain`、`--cutoff`）。mod-cap適用後は`--cutoff`でほぼ差が
出なくなった＝ノイズは出力高域でなく変調そのものが原因だったことの裏付け。

### 4.4 未解決事項

- OPQのオペレーターにOPMのslot1↔2インターリーブがあるか未検証（参照ソースなし）。現状は
  operator順を無変更のまま使用、通し再生で目立つ音程異常は出ていない。
- OPQ row6 bit6（波形ビット）が1の音色が全68音色中25件存在するが、具体的な波形は未確認。
  現状waveform=0（サイン波）固定。
- 音色名がROMから取得できず「PSR70 NN」のまま。

---

## 5. OPN / VGM — `vgm2x6`

VGM形式（YM2608/YM2612等のOPN系レジスタダンプ）を38x6へ変換する。

### 5.1 YM2608プリスケーラ問題（解決済み）

OPNベース音程ズレの真因はYM2608のプリスケーラだった（修正済み）。

### 5.2 feedback上限1.8とのトレードオフ（未解決）

2章参照。OPN変換のFMベースで、エンジン共通のfeedback上限1.8がFB=7のピッチを溶かす
（自己相関ピーク<0.3で98.8%がノイズ的）ことが判明。実機相当の0.5に戻すと72.4%が明瞭な
ピッチに乗るが、同じ1.8はハイハット系OPN音色（mucom2x6）の再現に必要——同じFB=7で
正反対の要求。対応は2章の「フィードバック上限1.8」参照。

**副産物**: このベースはキャリアMUL=2と7（MUL=1のキャリアが無い）で、両者の最大公約数
（基音134Hz=note48）が知覚ピッチになる（ミッシング・ファンダメンタル）。「オクターブ上に
聞こえるはず」という直感は誤りだった。

### 5.3 診断ツール

`--dump-pitch`（OPM/OPN共通スキーマでfreq/midi/note/pb/alg/carrier_muls等を出力）、
`--fb-max`（フィードバック上限の実験的上書き）、`--only-ch`（チャンネル分離）を実装済み。

### 5.4 vgm2op505（.38x6非経由の直接変換）

`op505/tools/vgm2op505`は、VGM/VGZを中間の`.38x6`を経由せず`.op505`（OP505のN点Time/Level
方式EG）へ直接変換する。他の直接変換ツール（`opz2op505`等）と異なり、vgm2x6は音色変換だけで
なく演奏ロジック本体（VGM逐次デコード・OPM/OPN二系統・SSGコアレス処理）が主体だったため、
新数式は書かずに**演奏ロジックの共有**で対応した:

- `ym38x6/tools/vgm2x6`をlib+binの2ターゲット構成へ切り出し、チップ判定・SMF/WAV出力抽象
  （`OpnSink`/`WavEngine`）・OPM/OPN演奏ループを`PatchConverter`トレイトでパッチ型非依存に
  ジェネリクス化した（`vgm2x6::patch::PatchBank<C>`/`vgm2x6::play`）。vgm2op505はこの
  libへ薄いバイナリとして相乗りし、`Op505Converter`（opm2op505/mucom2op505の
  `voice_to_op505_patch`へ委譲）と`Engine505`（`Op505Engine`のnewtypeラッパー）を
  差し込むだけで済む
- **バンク重複判定キーの統一が要**: 元のvgm2x6は非OPMエントリー（OPNボイス・SSG合成パッチ）
  の重複判定を「`.38x6`変換後のパッチ値」で行っていた（OPNボイスは`find_or_insert_fixed`
  経由）。直接変換でもこの判定基準を`Ym38x6Patch`のまま統一しないと、x6変換で潰れる2音色が
  変換器によって別エントリーになり、SMFのProgram Change番号がvgm2x6と食い違う。
  `PatchBank::find_or_insert_opn`（キー=`voice.to_ym38x6_patch()`）を新設し、
  `find_or_insert_fixed`と同一プールを共有させることで解決した
- SSG合成パッチ（`psg_patch`/`noise_patch`/`mix_patch`、`ssg.rs`）はx6ネイティブ定義の
  人工パッチ（実機レート由来ではない）なので、実機レート写像を新設せず
  `op505_core::adapter::convert_patch`（既存の`.38x6`→`.op505`2段変換）をそのまま使う
- Pitch/Cutoff/Gain FGはOP505ネイティブ既定値（他の直接変換ツールと同じ仕様）のため、
  `--attack bias`時の回帰テストは「operators全フィールド + channelの非FGフィールド」で
  ビット一致を確認する（FG差異は意図的、可聴上はノーオペ）
- `--dump-pitch`（ピッチロジック共通で新情報がない）と`--fb-max`/`--fb-2sample`/
  `--fb-1sample`（op505-coreはフィードバック実験フラグを搭載しない設計）はvgm2op505に
  持たせず、vgm2x6専用のまま残した

---

## 6. MUCOM88 / OPN — `mucom2x6`

MUCOM88（PC-88 MML）のOPN音色を38x6へ変換する。2章の「フィードバック上限1.8」「KSRクランプ」
の発見元。fmgen原音（`mucom88_slot{001,077,113}.wav`）を基準にオンセット窓0.3秒の
ZCR/輝度（diff energy比）を測定する検証手法を確立し、最終判断は聴感で行った。

---

## 7. 用語・記号早見表

| 記号/用語 | 意味 |
|---|---|
| `V_TL` | 実機TLレジスタへ最終的に書き込まれる値（0=最大出力、127=最小） |
| `A_ol` | Operator Output Level（VCED/VMEMの音色データ）由来の減衰 |
| `A_alg` | アルゴリズム（並列キャリア数）由来の追加減衰 |
| `A_vol` | Performanceモードのチャンネル音量由来の減衰（単一ボイス変換では非対象） |
| `A_ls` | Level Scaling（キーボード位置）由来の減衰 |
| `A_kvs` | Key Velocity Sensitivity（打鍵の強さ）由来の減衰 |
| `A_ebs` | EG Bias Sensitivity（ブレスコントローラ）由来の減衰 |
| Aol/Aalg等の単位 | TX81Z系はTLレジスタ7bit・0.75dB/step。38x6は8bit・0.373dB/step（ちょうど2倍解像度） |
