# 作曲支援アプリ UI設計仕様

## コンセプト

- **3画面構成のミニDAW**（CHORD / RHYTHM / MELODYタブ、画面切り替えは`gesture-app/src/screens.js`）
- コード画面は**フロー方式**：中央に現在のコード、左に選択履歴（過去のコード）、右に次の候補を表示し、
  選ぶたびに左へ流れていく（2026-09、旧キャリブレーション方式・旧グリッド方式から刷新。詳細は
  下記「コード画面: フロー方式」節）
- **マウス操作が前提**（デスクトップ、`gesture-app`のWindows/Tauri実装）。タッチ対応（タブレット）は
  今後の検討課題で、下記「ジェスチャーレパートリー」等は将来構想として残す

## コード画面: フロー方式

```
過去の履歴（最大4個、選んだ順）   現在のコード       次の候補（cols×rows、既定3列×5行）
  ┌───┐                    ┌───┐        ┌───┬───┬───┐
  │ G │ ← 遠いほど暗い       │     │        │緑 │黄 │灰 │ ← 上ほど高スコア
  └───┘  ┌───┐             │ C  │        ├───┼───┼───┤
         │ E │             └───┘        │ … │ … │ … │
         └───┘                          └───┴───┴───┘
  ← クリックでそこへ戻る                      定番 応用 自由枠
```

- 候補は直前のコードから見た理論スコア（`theory.js`の`classifyProgression`、1手目は`classifyInitial`）で
  緑（定番）→黄（用例は少ないが理論的裏付けあり）→灰（スコアが低い自由枠）の順に並ぶ
- 候補セルをクリックすると保持時間に関わらず選択が確定し、押している間だけ発音する
  （セル内の縦位置でベロシティ、上側ほど強い）
- 過去コードをクリックするとその地点まで即座に巻き戻る
- **Ctrl+Z**で1つ戻る、**Ctrl+Y**でやり直す（発音はしない）。戻ってから新しいコードを選ぶと、
  その先の履歴は破棄される（分岐は保持しない上書き方式）
- **Shift**＝4和音中心レイヤー、**Ctrl**＝sus・付加音系レイヤー、**Ctrl+Shift**＝aug・オルタード系
  レイヤーへ切替（`gesture-app/src/chords.js`の4レイヤー定義）
- 候補の行数（3〜12）・列数（1〜3）は右下パネルまたは↑↓←→キーで調整できる

キャリブレーション座標系・XY平面によるコード判定（初期案）およびグリッド方式（縦=コード種類・
横=半音、2026-09-06実装）はいずれも廃止済み。

## 統一UIアーキテクチャ

```
入力レイヤー
  Mouse:  mousedown/mousemove → 候補セル・過去コードセルの判定

          ↓

コード補助エンジン（共通、theory.js）
  直前のコード → 理論スコア → 緑/黄/灰の候補分類・並び替え

          ↓

音源エンジン（op505-standalone、Vco実装）
  選択されたコード → MIDI note_on/note_off → 発音

          ↓

UI描画（共通）
  現在のコード・過去の履歴・候補ブロックをCanvasへ描画
```

**チャンネルIDの割り当て：** コードの各声部には0〜N-1の固定スロット番号を`channel`として割り当て、声部インデックスとして使い続ける（[spec-sound.md](spec-sound.md)のキーオン契約参照）。コードを離して弾き直す際も同じスロットIDへ`note_on`するため、直前のリリーステールが同音チョークされ次の発音にかぶらない。声部数が減った場合、余ったスロットは`note_off`する。

## 動作モード

**モード1: コード録音**
```
1. リズムトラックを選択してテンポを設定（カシオ的なパターン）
2. リズム再生開始
3. 画面をタップ/ジェスチャーでコードを入力
   → タイムライン上に「この拍でCmaj、この拍でFmaj...」と記録
4. 記録完了
```

**モード2: メロディ即興**
```
1. 記録したコード進行 + リズムを再生
2. 現在再生中のコードが自動的に切り替わる
3. 画面を指でなぞる
   → 現在のコードのスケール音がリアルタイムで鳴る
   → コードが変わると使える音程も自動的に変わる
4. ∞ジェスチャーで装飾音・ビブラートを表現
```

両モードとも**同一のキャリブレーション・同一のジェスチャーシステム**を使用。

## Bank/Program（動作確認用の簡易UI）

画面右下の`#program-panel`（`gesture-app/src/index.html`）に、動作確認目的の簡易UIとして
Bank/Program選択を用意している（2026-08-20、ym38x6削除に伴いエンジン切替UI自体は廃止。
現在はop505単体）。

```
Bank <input> Program <input>
  └ op505_set_program(bank, program) を呼び、op505_presets_dir()配下の.op505プリセットを
    直接引く（フォールバックなし。見つからなければ現在の音色を維持し「未登録」と表示）
波形メモリ <checkbox>
  └ ONでBank欄をWAVEFORM_MEMORY_BANK(16383)に固定（op505には波形メモリ専用音色バンクが
    未移植のため、現状は「未登録」表示になる。将来op505へ移植する可能性を見越してUIは残してある）
```

Bank/Program変更時、`op505_set_program`がBank Select(CC0/32) + Program Changeを送る
（2026-09-01のMIDI送信化以降、gesture-app自体は音色エディタを持たない。編集は
`op505-standalone`のトレイ起動音色エディタで行う、CLAUDE.md gesture-app節参照）。

## ジェスチャーレパートリー

| ジェスチャー | 表現 | 実装 |
|---|---|---|
| 静止タップ | 単音・コード | 座標 → 音程に変換 |
| ゆっくり移動 | ポルタメント | 移動先座標でNote-On + CC65（Portamento ON）+ CC5（移動速度に応じたPortamento Time） |
| 横∞（∞）を描く | ビブラート・装飾音 | 軌跡がそのままピッチに追従 |

ジェスチャーに専用の認識アルゴリズムは不要。すべて「座標 → 音程」の連続マッピングで統一。∞の往復運動が自然にビブラートになる。

**∞ジェスチャーのパラメーター:**
```
∞の大きさ   → 装飾の幅（音程の揺れの深さ）
∞の速さ     → 装飾の速さ（ビブラートのレート）
∞の中心位置 → 基準音程
```

**FGとの関係:**
∞ジェスチャーによる装飾・ビブラートはF-Numberの直接制御（連続更新）であり、Pitch FGは使わない。
ゆっくり移動によるポルタメントはCC65/CC5を介した合成エンジン側のグライドで実現するため、こちらもFGとは無関係。
Pitch/Cutoff/Gain FGはジェスチャーとは独立した自動ビブラート/トレモロ/オートワウ等の別用途に使用可能
（ループ区間にtextureを指定すればS&H/Random/Chaosの不規則な揺れも表現できる）。
なお、一般的なモジュレーションホイール（CC1）は③ジェスチャー層としてPitch FG Depthへ瞬間加算する経路だが、本アプリはホイールを用いず∞ジェスチャーのF-Number直接制御を採るため、両者は別系統である。

## リアルタイムピッチ追従の要件

```
タッチイベントのサンプリング: 60〜120Hz（iPadのProMotionなら120Hz）
F-Numberの更新:             フレームごと（≒16ms以内）
音程変化のスムージング:      急激な変化を補間してなめらかにする
                            ただしレスポンスを損なわない程度に
```

## ターゲットデバイス

| デバイス | 画面 | 入力 | 評価 |
|---|---|---|---|
| iPadタブレット | 大 | マルチタッチ | 本命 |
| Androidタブレット | 大 | マルチタッチ | 有力な代替 |
| iPhone | 小（4本指で画面いっぱい） | マルチタッチ | 制約大 |
| Windowsデスクトップ | 大 | マウス | 開発・検証環境 |

## OP単位F-Numberの活用
- 1チャンネル（1ノート）内で、オペレーターごとに異なる周波数を割り当て可能
- 例：スケール音とアボイドノートを異なるOpに割り当てて同時に鳴らす

エンジン側の実装詳細は[spec-sound.md](spec-sound.md)のMIDI実装方針 / NRPN「Operator F-Number」を参照。

## オペレーター独立キーオンの活用
```
アボイドノートをタップした場合:
  全Op（0〜3）: 音量を絞った状態でキーオン（全OP対等・マスターOPは無い）
  → 「鳴るが控えめ」という警告的発音が可能
  ※ノート全体の停止は通常のNote-Offで行う（Op3をマスターとする特別扱いは廃止済み）
```

エンジン側の実装詳細は[spec-sound.md](spec-sound.md)のMIDI実装方針 / Operator Key On/Offセクションを参照。

## アボイドノートの挙動モード（作曲支援アプリ側の設計）
```
モード1: スナップ      → 最近傍のスケール音に自動補正
モード2: ランダムシフト → 上か下のスケール音にランダム移動
モード3: 無音          → 発音しない
モード4: 警告発音       → 音量を絞って発音（独立キーオン活用）
```

## 禁則コード進行の回避

前のコードの状態を記憶し、次のコードに遷移制約をかける。
ジェスチャーが禁則進行を引き起こす場合はスナップで補正。
