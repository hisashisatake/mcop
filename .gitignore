gesture-app/node_modules/
gesture-app/src-tauri/gen/
.vscode/
docs/
# 引継ぎ資料の置き場。コミット対象外（個人の作業環境に依存するため）。
handoff/
# 変換ツールの入出力データ置き場（コミット対象外）
ym38x6/tools/*/private/
op505/*/private/
op505/tools/*/private/
# Claude Codeの個人設定（権限allowリスト等。共有しない）
.claude/settings.local.json
**/__pycache__
**/target
*.lock
# op505-mme-driver（x64/x86 op505mme.dll）のビルド成果物置き場。install-mme-driver.ps1が
# 参照するが、target/同様にビルドで再生成されるためコミット対象外。
op505/mme-driver/dist/
# NSISインストーラ（op505-setup.exe）のビルド成果物。build-installer.ps1で再生成されるため
# コミット対象外。
op505/mme-driver/installer/dist/
