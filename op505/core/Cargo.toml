[package]
name = "op505-core"
version = "0.1.0"
edition = "2021"

[dependencies]
sound-core = { path = "../../sound/core" }
sound-fm = { path = "../../sound/fm" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
# examples/op505_probe.rsが既存.38x6からのAdapter変換に使う（製品コードのsrc/は依存しない）。
ym38x6-core = { path = "../../ym38x6/core" }
