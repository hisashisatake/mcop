[package]
name = "opzref"
version = "0.1.0"
edition = "2021"

# ymfm OPZ(YM2414)を参照レンダラとして使う検証用ツール。
# ymfm本体(BSD-3-Clause)は vendor/ymfm に同梱し、cc でC++をビルドしFFIで呼ぶ。

[[bin]]
name = "opzref"
path = "src/main.rs"

[dependencies]
opz2x6 = { path = "../opz2x6" }

[build-dependencies]
cc = "1"
